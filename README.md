# JM-Optimizer
JM Optimizer is a Python-based desktop application that optimizes Bill of Materials (BoM) against available stock to generate efficient production plans. Built with Tkinter, Pandas, and PuLP, it provides a simple interface for uploading data, running optimization, and exporting results to reduce manual planning errors.
